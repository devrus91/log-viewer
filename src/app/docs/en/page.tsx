import type { Metadata } from "next";
import Link from "next/link";
import { ArrowLeft, BookOpen, Braces, Gauge, Sigma } from "lucide-react";
import { WotLabBrand } from "@/components/WotLabBrand";

export const metadata: Metadata = {
  title: "Documentation — WOT Lab",
  description: "WOT Lab guide to calculated channels, analysis rules, and window functions.",
};

const WINDOW_FUNCTIONS = [
  { name: "lag(expr, N)", result: "The expression value N samples earlier.", edge: "The first N values are undefined." },
  { name: "delta(expr, N)", result: "The current value minus the value N samples earlier.", edge: "The first N values are undefined." },
  { name: "moving_avg(expr, N)", result: "Average of the latest N samples, including the current sample.", edge: "Requires a complete window without gaps." },
  { name: "moving_min(expr, N)", result: "The smallest value among the latest N samples.", edge: "Requires a complete window without gaps." },
  { name: "moving_max(expr, N)", result: "The largest value among the latest N samples.", edge: "Requires a complete window without gaps." },
  { name: "moving_sum(expr, N)", result: "Sum of the latest N samples.", edge: "Requires a complete window without gaps." },
];

const SCALAR_FUNCTIONS = [
  ["abs(x)", "absolute value"], ["min(a, b, …)", "smallest value"], ["max(a, b, …)", "largest value"],
  ["avg(a, b, …)", "argument average"], ["sqrt(x)", "square root"], ["pow(x, y)", "x raised to y"],
  ["clamp(x, min, max)", "range limit"], ["round(x)", "round to nearest"], ["floor(x)", "round down"],
  ["ceil(x)", "round up"], ["if(condition, yes, no)", "conditional choice"],
];

function CodeExample({ children }: { children: string }) {
  return <pre className="docs-code"><code>{children}</code></pre>;
}

export default function EnglishDocumentationPage() {
  return <main className="docs-page" lang="en">
    <header className="docs-topbar"><Link href="/" aria-label="WOT Lab home"><WotLabBrand compact /></Link><div className="docs-topbar-actions"><span>LOG VIEWER GUIDE</span><nav className="docs-language-switcher" aria-label="Documentation language"><Link href="/docs" lang="ru">RU</Link><Link className="active" href="/docs/en" lang="en" aria-current="page">EN</Link></nav><Link className="docs-back-link" href="/"><ArrowLeft size={15} /> Back to the app</Link></div></header>
    <div className="docs-shell">
      <aside className="docs-nav"><div><BookOpen size={17} /><span><b>Documentation</b><small>Formulas and diagnostics</small></span></div><nav aria-label="Documentation sections">
        <a href="#quick-start">Quick start</a><a href="#formula-language">Expression language</a><a href="#calculated-channels">Calculated channels</a><a href="#diagnostic-rules">Analysis rules</a><a href="#window-functions">Window functions</a><a href="#channel-mapping">Channel matching</a><a href="#recipes">Ready-to-use examples</a>
      </nav><p>All calculations run on your device. Log data is not sent to a server.</p></aside>

      <article className="docs-content">
        <section className="docs-hero"><span className="eyebrow">WOT LAB · USER GUIDE</span><h1>Formulas and rules<br /><em>you can trust.</em></h1><p>A practical guide to building calculated signals and analyzing automotive CSV logs.</p><div className="docs-hero-cards"><a href="#calculated-channels"><Sigma size={18} /><span><b>Calculated channels</b><small>Derived signals</small></span></a><a href="#diagnostic-rules"><Gauge size={18} /><span><b>Analysis rules</b><small>Events inside WOT areas</small></span></a><a href="#window-functions"><Braces size={18} /><span><b>Window functions</b><small>History and smoothing</small></span></a></div></section>

        <section id="quick-start" className="docs-section"><div className="docs-heading"><span>01</span><div><h2>Quick start</h2><p>Calculated channels and analysis rules use the same expression language.</p></div></div><div className="docs-steps"><article><b>Calculated channel</b><ol><li>Open a CSV log.</li><li>Select <strong>Formula</strong> or press <kbd>Ctrl/⌘+F</kbd>.</li><li>Enter a name, unit, and expression.</li><li>Select <strong>Create channel</strong>.</li></ol></article><article><b>Analysis rule</b><ol><li>Open <strong>Settings → Diagnostics</strong>.</li><li>Select <strong>Configure diagnostic rules</strong>.</li><li>Select <strong>New rule</strong>.</li><li>Configure the conditions and run the analysis again.</li></ol></article></div><div className="docs-note"><b>What they share</b><span>Both support arithmetic, parameters, scalar functions, and window functions. Only channel notation differs: a calculated channel uses the name from the log, while a rule uses a standard WOT Lab name.</span></div></section>

        <section id="formula-language" className="docs-section"><div className="docs-heading"><span>02</span><div><h2>Shared expression language</h2><p>These features are available in both calculated channels and rule conditions.</p></div></div><div className="docs-syntax-table"><div><b>Channel</b><code>[Engine Speed (rpm)]</code><span>A channel name inside square brackets</span></div><div><b>Parameter</b><code>MIN_RPM</code><span>Letters, digits, and _, not starting with a digit</span></div><div><b>Number</b><code>0.5 · .5 · 1e-3</code><span>Decimal or exponential notation</span></div><div><b>Arithmetic</b><code>+ − * / % ^</code><span>Standard precedence; parentheses change evaluation order</span></div><div><b>Comparison</b><code>&gt; &gt;= &lt; &lt;= == !=</code><span>Returns 1 when true and 0 when false</span></div><div><b>Logic</b><code>AND · OR · NOT</code><span>Combines multiple conditions</span></div></div><h3>Scalar functions</h3><div className="docs-function-grid">{SCALAR_FUNCTIONS.map(([signature, description]) => <div key={signature}><code>{signature}</code><span>{description}</span></div>)}</div><p className="docs-muted">Channel names must match those shown by the application. Division by zero and insufficient data produce an undefined value.</p></section>

        <section id="calculated-channels" className="docs-section"><div className="docs-heading"><span>03</span><div><h2>Calculated channels</h2><p>A new channel is calculated for every sample in the source log.</p></div></div><div className="docs-callout"><Sigma size={18} /><div><b>How to reference a source channel</b><p>Put the exact log channel name, including spaces and units, inside square brackets: <code>[Boost Pressure Actual (bar)]</code>. The easiest method is to click the required channel on the right side of the editor so the reference is inserted automatically.</p></div></div><h3>Formula editor fields</h3><div className="docs-definition-grid"><div><code>Channel name</code><p>A unique name for the new channel. Other calculated channels can reference it.</p></div><div><code>Unit</code><p>The unit displayed on the chart. Units are not converted automatically.</p></div><div><code>Expression</code><p>The expression calculated for every sample.</p></div><div><code>Reusable parameters</code><p>Named numeric values, for example <code>BAR_TO_PSI = 14.5038</code>.</p></div></div><h3>Basic example</h3><CodeExample>{`Name: Boost Error\nUnit: bar\nExpression: [Boost Pressure Target (bar)] - [Boost Pressure Actual (bar)]`}</CodeExample><p>WOT Lab remembers created channels and attempts to restore them when later logs are opened. If a required source channel is missing or has a different name, the result cannot be calculated. Circular dependencies, where channels directly or indirectly reference each other, are not allowed.</p><div className="docs-note"><b>Window functions are supported</b><span>A calculated channel expression can use <code>lag</code>, <code>delta</code>, <code>moving_avg</code>, <code>moving_min</code>, <code>moving_max</code>, and <code>moving_sum</code>. See <a href="#window-functions">Window functions</a> for details.</span></div><div className="docs-note"><b>How to edit a channel</b><span>Select the calculated channel on the chart and use <strong>Edit selected</strong>, or use the pencil icon next to it in the channel list. Saving also recalculates dependent channels.</span></div></section>

        <section id="diagnostic-rules" className="docs-section"><div className="docs-heading"><span>04</span><div><h2>Log analysis rules</h2><p>A rule finds continuous areas in which its configured conditions are true.</p></div></div><div className="docs-callout"><Gauge size={18} /><div><b>Formulas also work in rules</b><p>Either side of a condition can use parameters, arithmetic, scalar functions, and every window function. For example: <code>moving_avg([boost.target] - [boost.actual], 10)</code> <strong>&gt;</strong> <code>MAX_ERROR</code>.</p></div></div><h3>How to create a rule</h3><ol className="docs-process"><li><span>1</span><div><b>Open the editor</b><p>Go to <strong>Settings → Diagnostics → Configure diagnostic rules</strong>, then select <strong>New rule</strong>.</p></div></li><li><span>2</span><div><b>Add conditions</b><p><strong>All conditions</strong> requires every condition; <strong>Any condition</strong> requires at least one.</p></div></li><li><span>3</span><div><b>Set the duration</b><p>Duration removes short areas, while Cooldown merges nearby areas.</p></div></li><li><span>4</span><div><b>Restrict the full-throttle area</b><p>With <strong>WOT area only</strong> enabled, only areas inside detected WOT pulls are considered.</p></div></li><li><span>5</span><div><b>Run the analysis</b><p>After saving, select <strong>Re-run diagnostics</strong> to apply the changes to the current log.</p></div></li></ol><h3>Main fields</h3><div className="docs-definition-grid"><div><code>Severity</code><p>The importance of a detected event: info, warning, or critical.</p></div><div><code>WOT area only</code><p>Keeps results only inside automatically detected full-throttle areas.</p></div><div><code>Duration, ms</code><p>Minimum time for which the conditions must remain continuously true.</p></div><div><code>Cooldown, ms</code><p>Largest gap across which two areas are merged.</p></div><div><code>Correlation window, ms</code><p>The interval used to associate events that occur close together.</p></div><div><code>Parameters</code><p>Threshold values for conditions. Double-click a name to edit it.</p></div><div><code>Recommended channels</code><p>Channels that are useful to display alongside the detected event.</p></div><div><code>Message / Possible causes</code><p>The result description and possible causes of the issue.</p></div></div><h3>Example rule for a WOT area</h3><CodeExample>{`Name: Boost under target\nWOT area only: enabled\n\nCondition 1:\n  [boost.target] - [boost.actual]  >  MAX_ERROR\nCondition 2:\n  [engine.rpm]  >  MIN_RPM\n\nParameters:\n  MAX_ERROR = 0.25\n  MIN_RPM = 2500\nMinimum duration = 200 ms\nMerge gaps up to 300 ms`}</CodeExample><p>To suppress single-sample spikes, replace the first condition with <code>moving_avg([boost.target] - [boost.actual], 10) &gt; MAX_ERROR</code>. If a required channel is unavailable, the rule cannot evaluate that condition.</p><div className="docs-warning"><b>WOT rules depend on detected pulls.</b><span>If WOT Lab cannot detect a full-throttle area, rules with WOT area only enabled produce no events.</span></div></section>

        <section id="window-functions" className="docs-section"><div className="docs-heading"><span>05</span><div><h2>Window functions</h2><p>A shared function set for calculated channels and analysis rules.</p></div></div><div className="docs-callout"><Braces size={18} /><div><b>Where they can be used</b><p>Every function listed below works both in a calculated channel expression and on either side of a diagnostic rule condition.</p></div></div><div className="docs-warning"><b>N is a sample count, not a time value.</b><span>At 50 Hz, a 10-sample window is approximately 200 ms. If samples have varying intervals, the actual window duration changes. N must be an integer from 1 through 10,000; a parameter can be used instead of a number.</span></div><div className="docs-window-table"><div className="head"><span>Function</span><span>Result</span><span>Start of the log</span></div>{WINDOW_FUNCTIONS.map((item) => <div key={item.name}><code>{item.name}</code><span>{item.result}</span><small>{item.edge}</small></div>)}</div><h3>In a calculated channel</h3><CodeExample>{`// Smoothed boost error over the latest 10 samples\nmoving_avg([Boost Target] - [Boost Actual], 10)\n\n// RPM change relative to 5 samples earlier\ndelta([Engine Speed (rpm)], 5)`}</CodeExample><h3>In a rule condition</h3><CodeExample>{`Left side:  moving_avg([boost.target] - [boost.actual], WINDOW)\nOperator:   >\nRight side: MAX_ERROR\n\nParameters: WINDOW = 10, MAX_ERROR = 0.25`}</CodeExample><p>For <code>lag</code> and <code>delta</code>, the first N rows do not have enough history. A <code>moving_*</code> function starts producing results after its first complete window. A missing value inside the window makes that window&apos;s result undefined.</p></section>

        <section id="channel-mapping" className="docs-section"><div className="docs-heading"><span>06</span><div><h2>Why channel names differ</h2><p>Rules use standard names so one rule can work with logs from different manufacturers.</p></div></div><div className="docs-mapping-flow"><code>Boost Pressure Actual (bar)</code><span>channel matching</span><code>[boost.actual]</code></div><p>A calculated channel references the exact column name in the current file. An analysis rule uses a short, standard WOT Lab name. WOT Lab usually finds the corresponding column automatically.</p><p>If the wrong column is selected or your logger&apos;s name is not recognized, open <strong>Settings → Diagnostics → Map channels</strong> or select <strong>Map channels</strong> in the rule editor and choose the match manually.</p><div className="docs-note"><b>Units are not converted</b><span>A rule threshold must match the real channel unit. For example, a threshold in bar cannot be applied to a kPa value without adjustment.</span></div></section>

        <section id="recipes" className="docs-section"><div className="docs-heading"><span>07</span><div><h2>Ready-to-use examples</h2><p>Replace the names in square brackets with channel names from your own log.</p></div></div><div className="docs-recipes"><article><span>BOOST</span><h3>Boost error</h3><CodeExample>{`[Boost Target] - [Boost Actual]`}</CodeExample></article><article><span>FUEL</span><h3>Lambda error</h3><CodeExample>{`[Lambda Actual] - [Lambda Target]`}</CodeExample></article><article><span>SMOOTHING</span><h3>Smoothed signal</h3><CodeExample>{`moving_avg([Fuel Pressure Actual], 20)`}</CodeExample></article><article><span>CHANGE</span><h3>Windowed change</h3><CodeExample>{`delta([Engine Speed (rpm)], 5)`}</CodeExample></article><article><span>LIMIT</span><h3>Range limit</h3><CodeExample>{`clamp([Throttle Position], 0, 100)`}</CodeExample></article><article><span>CONDITION</span><h3>Conditional signal</h3><CodeExample>{`if([Engine Speed] > MIN_RPM, [Boost Actual], 0)`}</CodeExample></article></div></section>

        <footer className="docs-footer"><WotLabBrand compact /><span>WOT Lab guide to formulas and log analysis.</span><Link href="/"><ArrowLeft size={14} /> Open the log viewer</Link></footer>
      </article>
    </div>
  </main>;
}
